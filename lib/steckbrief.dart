import 'package:flutter/material.dart';

class SteckBrief extends StatelessWidget {
  const SteckBrief({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(title: const Text('Steckbrief Lama')),
     // body: Image.asset('assets/images/lama.png'),
    );
  }
}